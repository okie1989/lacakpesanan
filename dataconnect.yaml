specVersion: "v1beta"
serviceId: "app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
